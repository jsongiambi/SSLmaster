// var m = require('./model.js');

// // Get only data that has flag set to .. true
// m.shareData.find({ "page": "Veteran's Hangout"}, 'links', function (err, data) {
//     if (err) return console.error(err);
//     for (var i = 0; i < data.length; i++) {
//         // Update entry setting flag to false -- query entry by id then set flag to false
//         for (var y = 0; y < data[i].links.length; i++) {
//             // If page not in any entry create and set value to 1 -- otherwise update entry to count++
//         }
//     }
// })